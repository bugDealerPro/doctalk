[project]
# Chainlit project config placeholder; app uses defaults.

[UI]
name = "DocTalk"
description = "Upload a PDF and chat with its contents."

[features]
spontaneous_file_upload = { enabled = true, accept = ["application/pdf"], max_files = 10, max_size_mb = 20 }

[meta]
generated_by = "1.0.0"
